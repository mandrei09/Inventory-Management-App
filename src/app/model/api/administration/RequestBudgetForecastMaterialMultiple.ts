export class RequestBudgetForecastMaterialMultiple {
    id : number;
    multiple : boolean;

    constructor(id:number,multiple:boolean)
    {
        this.id = id    
        this.multiple = multiple
    }
}
